FROM armor_auto
# WORKDIR /app

# COPY BUILD /root

# RUN apt-get update && \
# RUN cd /app/BUILD && ./auto_config.sh
# RUN echo "source /opt/ros/humble/setup.bash" >> /root/.bashrc
# RUN /bin/bash -c "source /root/.bashrc"
